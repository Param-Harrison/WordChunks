<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imgSettingsIcon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:layout_gravity="start"
        android:layout_margin="12dp"
        android:contentDescription="@string/settings_hint"
        android:src="@drawable/ic_settings_white_24dp" />

    <!--
    Kitty image from:
    http://randomrobskii.deviantart.com/art/Kitty-Cat-408720148
    -->
    <ImageView
        android:id="@+id/imgKitty"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_weight="2"
        android:adjustViewBounds="true"
        android:contentDescription="@string/app_name"
        android:src="@drawable/cat" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:text="@string/app_name"
        android:textAlignment="center"
        android:textColor="#ffffff"
        android:textSize="24sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <Button
            android:id="@+id/btnPlay"
            style="@style/PlayButtonStyle" />

        <Button
            android:id="@+id/btnDaily"
            style="@style/DailyButtonStyle" />

        <Button
            android:id="@+id/btnPacks"
            style="@style/PacksButtonStyle" />

        <Button
            android:id="@+id/btnShop"
            style="@style/ShopButtonStyle" />

    </LinearLayout>

</LinearLayout>